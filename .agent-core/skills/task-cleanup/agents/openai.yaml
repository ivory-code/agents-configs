interface:
  display_name: "Task Cleanup"
  short_description: "Safely remove completed task worktrees and branches"
  default_prompt: "Use $task-cleanup to safely remove this completed task's worktree, directory, and local and remote branches."
